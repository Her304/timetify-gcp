# CLAUDE.md

**Frontend:** `npm run dev` (port 5173), `npm run build`, `npm run lint`
**Backend:** `python manage.py runserver` (port 8000), `migrate`, `makemigrations`

## Architecture

**Layout:** `frontend/` (React+Vite+Tailwind), `backend/` (Django); single `main/` app
**State:** All in `App.jsx`, passed as props. React Router v6 with `ProtectedRoute`.
**Auth:** JWT in localStorage, auto-refresh via `authenticatedFetch`. Password reset via Django's `PasswordResetView` at `/password_reset/`.
**Course upload:** `add.jsx` → `/api/courses/analyze/` → GPT-4o-mini parsing → `/api/courses/finalize/` persists. **Constraints:** 5MB max, `.pdf`/`.docx` only, magic-byte check, server-generated filename. Keep all four checks if editing; `pdf.extract_text` raises on unknown extensions.
**Home page (`/`):** `home/week_view.jsx` — Mon–Sun calendar grid (left) + side rail (right). Header reads `WEEK OF <date>` mono label + `ur week, ur ppl` Bricolage title + a live coral `<DAY> · h:mm AM` clock pill (updates every 30s via `setInterval`) + lighter `N friends live now` pill + `+ add class` dark pill → `/Add`. Day columns use a stable palette keyed by hash of `course_id`; current day column gets a coral tint; live class blocks get a coral ring; the "now" line is a coral horizontal bar. Hour range auto-clamps to data (default 8–18, expands as needed). **Class blocks** are full-width clickable buttons (no 60/40 width split) — opening `CourseDetailsModal` with that single class; if height < 56px they switch to a vertical stack (course code on top, avatars on bottom) to keep all three rows readable. **Overlap chip:** when multiple classes overlap, the primary block renders a coral-outlined `+N` chip in its top-right corner (stops propagation); clicking it opens `CourseDetailsModal` in master-detail mode with the full cluster on the left and the selected class's details on the right. Side rail: dark **LIVE NOW** card with coral `Blob`, white **TODAY** card (mono `TODAY · DAY DATE` + class count, `HAPPENING NOW` coral sub-card with time range + course id + name + location, then `COMING UP` timeline with pin-and-connector), then white **REMINDERS FOR TODAY** card listing today's assignments (lime `DUE` chip) and exams (coral `EXAM` chip) — empty state `all clear today ✓`. Reminders read from `/api/courses/` fetched once on mount inside `WeekView`. `home/snap.jsx` is **dead code** since the snap block moved to `/feed`.

**Course details modal (`home/CourseDetailsModal.jsx`):** Lazy-fetches `/api/courses/` on open, keys by `course_id`. Two modes — single class (header banner only) or cluster master-detail (left rail of avatar+code+room rows, right pane is the selected entry). The right pane always shows: colored banner (course id + name + `DAY · h:mm–h:mm · location`) and a `who's in this` avatar-chip row. Sections below (`this week`, `upcoming` exams/assignments, `full course page →` link) **only render when the course has data** — if the matched Course has empty `weeks`/`exams`/`assignments` and `has_ai_content=false`, those sections are hidden entirely. Do not reintroduce a loading skeleton or `no extra details` empty state — they were removed because the matched course often has nothing extra and the placeholder was noise.

**Feed page (`/feed`):** `feed/feed.jsx` — replaces the old home-page snap block. Header reads `ur feed` + `THU · MAY 21` mono date (uppercase, `·` separator). **Pills are sort-priorities, NOT filters** — every accepted friend always shows in both the avatar row and inbox; the pill just decides who's surfaced first (`today` → snapped-today first, `my_classes` → shared-course friends first, `friends` → snap-havers first). The pill row also holds a circular peach `N on` badge counting live snap-havers (last 30 min). **No** sort dropdown and **no** snap-card list — both were dropped along with the "showing N snaps" line; snaps are consumed only via the avatar row. **Avatar row:** `me + plus` tile first, then friends ordered by primary/rest where snap-havers get the coral ring and non-snap friends get a faded `ink-15` ring at 60% opacity; the rest-tail uses a session-stable seeded shuffle (`useState(() => Math.random())`). Click a snap-haver → `SnapViewerModal`; click a no-snap friend → `openChat(friend)` (create-or-get DM, then `navigate('/chat/<id>')`). **Me tile status:** derives a context class from `personalSchedule` (currently-happening, else next-up today) and renders class-aware mono text under the avatar — `snap for <code>` (coral) if I haven't snapped yet, or two stacked lines `snapped Xm ago` / `for <code>` if I have. Falls back to plain `u` when there's no class today. **Chat inbox** below the avatar row fetches `/api/chats/` on mount + on window focus into `chatsByFriendId` and merges with `orderedTiles`; rows render `avatar · username · preview · time chip` with a coral unread badge (`N` / `9+`) when `unread_count > 0`. Preview = last_message.content, or italic `[message removed]` when `is_removed`, or `snapped Xm ago` / `tap to chat` fallback. Always shows **all** friends regardless of pill. **Inbox sort:** unread → most recent `last_message.created_at` → snap-havers → alphabetical. Row click + no-snap avatar tile click share the same `openChat` handler (POST `/api/chats/` with `{friend_id}` — friend-gated dedup); inline spinner while in flight. **Data shape gotcha:** `friendsList` items are friendship rows (`{id, user, friend, status, friend_details}`), NOT user objects — flatten via `friendsList.map(f => f.friend_details).filter(Boolean)`. Server already filters to `status=1` (accepted). Camera/picker/viewer wiring matches the old snap block (course-picker modal when >1 own course). Do **not** reintroduce the old "messages coming soon" centered sheet — it was deleted when the real `/chat` route landed.

**Chat (`/chat/:roomId`):** `chat/ChatThread.jsx` — protected, 2-column layout on desktop (`md:flex-row gap-4`, `maxWidth: 1100`); mobile stacks the side panel below the chat column. **Chat column** (`md:max-w-[640px]`) is height-capped at `calc(100dvh - 120px)` so the message list and input bar stay confined while the aside scrolls independently on desktop. Header: back chevron → `/feed`, hash-colored `Avatar`, lowercase Bricolage username, mono `active Xm ago` / `last seen H:MM am` from `other_user.last_seen`. **Message list** uses `flex flex-col-reverse` so the local `messages` array stays in DESC order (matches API response shape) while rendering ascending — new messages prepend to index 0 and naturally land at the visual bottom with no manual scroll. Bubble: coral with white text for me, `T.ink08` for the other user, italic `[message removed]` at `T.ink40` for soft-deleted, dimmed 0.6 while `__pending`, `↻ retry` link when `__failed`. Per-message mono timestamp under the bubble for the newest message and any message where the gap to the next-older one is > 5 min. **Input bar**: autosizing textarea (1–5 lines), Enter sends / Shift+Enter newline, 2000-char hard cap (client clamps, server rejects 400), `chars left` counter at ≥1800 (turns coral within 50 of cap), inline paper-plane SVG (no `send` icon in brand atoms). Optimistic send appends a `pending-<ts>` row, replaced on 201 with the real row, flipped to `__failed` on error. **Load older** uses an `IntersectionObserver` on a sentinel placed last in DOM (= visual top under col-reverse) → `GET /api/chats/<id>/messages/?before=<oldest_id>`, merges by id, sets `olderExhausted` on empty response. **Polling**: 5 s `setInterval` while `document.visibilityState === 'visible'`, paused/resumed on `visibilitychange` + `focus`; merges by id so paginated older messages stay put. `markRead()` (POST `/read/`) fires on mount and whenever the poll observes a new message from the other user. Empty state: "say hi to @username 👋". Error states for `forbidden` / `not_found` / `network`. Both the mobile floating bottom-nav pill **and** the mobile top bar are **hidden on `/chat/<id>`** via a `location.pathname.startsWith('/chat/')` check in `AppShell` so neither covers the chat's own header/input. **Side panel** (right aside, ~`md:w-[320px]`) renders two cards from props threaded through the Route: `SharedClassesCard` intersects `allClasses` by `base_course` for today using my entry's `start_time/end_time/location` for display, highlighting the currently-in-session class with a coral fill + "now" chip (pure decor — no click handler); `TheirSnapsCard` filters `snapsByCourse` by `uploader_username === other_user.username` and `created_at >= startOfDay`, de-dupes by snap id, renders a 3-col 4:5 thumbnail grid (img for photos, muted/preload="metadata" video for video snaps, with `course_code` overlay chip), and clicking a thumb opens `SnapViewerModal` with `snaps={theirSnapsToday}` and `courseLabel='@username'`. Thumbnail URLs go through a local `resolveMediaUrl` helper mirroring the viewer's — relative `/media/...` paths get prefixed with `VITE_API_URL` in local dev; absolute GCS URLs pass through.

**Snap pipeline:** Add tile → 0 own courses noop, 1 straight to capture, >1 course-picker modal → `snap/SnapCaptureModal.jsx` (dark `T.ink` shell, 78px coral shutter, focus brackets, course pill on viewfinder; header title is dynamic: `snap <course code>` if current time falls inside the picked course's window via `isLiveNow`, else `snap now`; in-browser camera, `getUserMedia` 1080×1350, photo via canvas JPEG q=0.85, video via `MediaRecorder` 5s hard cap; mp4 on Safari, webm on Chrome — backend accepts both) → `POST /api/snaps/` (multipart). **Camera release:** parent's `onClose` runs `window.location.reload()` after `setCaptureCourse(null)` — relying on the React unmount cleanup alone left Chrome's camera indicator stuck. Feed: `GET /api/snaps/feed/` returns `snaps_by_course` keyed by Course PK; each snap now also includes `view_count` (count of `SnapAudience` rows with `has_viewed=True`; uploaders aren't counted because no self-audience row is written). Mark viewed: `POST /api/snaps/<id>/view/`. Uploader soft-delete: `DELETE /api/snaps/<id>/`. **Viewer (`SnapViewerModal`):** dark `T.ink` shell; receives `prevTile`/`nextTile`/`onSelectPrev`/`onSelectNext`/`onAdd` from the parent (parent tracks `viewerTileIdx` into `tiles`); renders side `Avatar`s absolutely-positioned on the backdrop — left = previous uploader (or the add icon if at the first tile, which closes the viewer and runs `handleAddClick`), right = next uploader. Internal chev-icon arrows still page within one uploader's snaps; `useEffect([snaps])` resets `idx` to 0 when switching uploaders. Header subline appends `· snap from <course>` when the snap's `created_at` HH:MM is inside that course's class window. **Constraints:** 20MB max, allowed types `image/jpeg|png` + `video/mp4|quicktime|webm`, magic-byte check, server-generated path `snaps/<user_id>/<uuid>.<ext>`, **4:5 portrait** (`TARGET_RATIO = 4/5`, both modal preview and viewer use `aspect-[4/5]`), **caption max 50 words** (server splits on whitespace + slices). Visibility: `'all_friends'` (no audience rows; feed joins through current `Friend` table — late-added friends can view) or `'selected'` (per-friend `SnapAudience` rows as allowlist). `SnapAudience` doubles as the view-tracker, lazy-created on first view for `all_friends`. `expires_at` = midnight of next day in the **user's IANA TZ** (browser sends `timezone` field; backend uses `zoneinfo`). **No** restriction-check on upload yet (lives in plan Phase 6/7). Schedule endpoint (`home()`) emits `id` (Course PK) per entry so the frontend can key snaps to cards.

**Friend page (`/friend`):** `friend/friend.jsx` — receives `allClasses` from `App.jsx` so it can derive "live now" friends (any class today where `nowMins` ∈ [start, end] and the owner isn't `Me`/`currentUser.username`). Header: mono `<N> FRIENDS · <M> IN CLASS NOW` + `ur ppl ◆` Bricolage title + inline search input (debounced 250ms) + dark `+ find more` button (scrolls to suggested panel). Filter pills with counts: `all · N` / `my classes · N` / `my year · N` / `live · N` (replaces the old `nearby` since there's no geo data) + sort dropdown (recently active / live first / name). **While the search input is empty** the page renders a 1fr/320px split: left = horizontal **LIVE NOW** dark strip (up to 5 friend tiles, each `avatar+username+course`) + `recently active` 3-col grid of `FriendCard`s (big avatar with coral ring if live, `LIVE NOW`/`RECENTLY` badge, course pills colored by `dotForCourse(courseId)`, `snapped Xm ago` from `last_snap_at` or `active Xm ago` from `last_seen`, dark `message` button (disabled — "messages coming soon") + cream calendar button). Right rail = `RequestsCard` (inline ✓/× accept-reject) + dark `SuggestedCard` with lilac `Blob` (calls `searchfriends(currentUser.major)` on mount). **Typing in search** swaps the whole grid for the existing compact `SearchResultRow` list. Incoming requests render only in the right-rail RequestsCard, not inline above results.

**Notifications:** `GET /api/notifications/` — fully computed, no stored model. Returns three keys: `friend_requests` (pending `Friend` rows where current user is recipient), `new_snaps` (active non-expired friend snaps with no `SnapAudience has_viewed=True` row for me), `live_class_alerts` (courses live right now — HH:MM string comparison matching frontend `isLiveNow` — where ≥3 friends share the same course; returns `{course_id, course_name, friend_count, friends[]}` capped at 5 friends). **`NotificationsPanel.jsx`** is the single panel component used by both surfaces via a `variant` prop: `variant="dropdown"` (default) — desktop popover anchored below the bell with the absolute-positioned `top-full mt-2 w-80` container; `variant="fullscreen"` — mobile sheet that fills its parent (`w-full h-full flex flex-col`) with a header × close button. Both variants share the same three sections, the same accept/decline pipeline (`onRespondToRequest` threaded from `App.jsx` → `AppShell` → header/top-bar), the same empty state `all clear ✓`, and the same coral bell badge counting `friend_requests.length + new_snaps.length` (live alerts excluded — ephemeral). The mobile shell is owned by `MobileTopBar.jsx`, which renders a backdrop overlay, locks `document.body.style.overflow` while open, and stops click propagation on the panel itself so background taps close it.

**Nav:**
- **Shared icon module** `application/app-navigation/nav-icons.jsx` — single source of truth for the Material Design SVGs used by both surfaces. Exports `NavIcon` (paths inherit `currentColor`); `NAV_ICON_PATHS` is a local const (not exported, to satisfy the react-refresh "components-only" lint rule). Both the desktop header pills and the mobile bottom pill import `NavIcon` from here so they stay visually identical.
- **Desktop (`hidden md:flex`):** `application/app-navigation/header-nav-app.jsx` — sticky top navbar with `AppMark` + wordmark on the left, three pill items `feed` / `schedule` / `friends` (active item = ink-on-cream pill), then the right cluster anchored via `ml-auto`: bell button (opens `NotificationsPanel` dropdown), **lilac** avatar circle (`T.lilac` bg / `T.ink` text) linking to `/profile`, **coral** `log out` pill (was ink — switched to coral to match the mobile profile button and signal a destructive action). The **`feed` pill** accepts a `badge` prop and renders a coral count chip when `unreadChatCount > 0` and the user isn't already on `/feed`. **No `settings` pill** anymore — the avatar circle is the single entry point to `/profile`. **No search input**. The old `sidebar-navigation/sidebar-simple.jsx` is **dead code** for logged-in users — do not re-wire it.
- **Mobile top bar (`md:hidden`):** `application/app-navigation/mobile-top-bar.jsx` — sticky cream bar at top (`top-0 z-30 h-14`, bottom border), `AppMark` + `timetify` wordmark on the left, bell button on the right with the same coral count badge as the desktop bell. Bell tap opens `NotificationsPanel variant="fullscreen"` inside a black/30 backdrop; tap backdrop to dismiss. Hidden on `/chat/<id>` so it doesn't compete with the chat thread's own header.
- **Mobile bottom (`md:hidden`):** `application/app-navigation/mobile-bottom-nav.jsx` — floating ink pill at `bottom-4` (`fixed`, `z-40`) with 4 items: `feed` / `schedule` / `friends` / avatar. The three icon items use the **same Material Design SVGs as desktop** (via shared `NavIcon`) — active tab gets a coral filled circle behind the icon, inactive cream. The 4th tab is an **AvatarTab**: lilac circle with the user's first initial (matches the desktop avatar). When `/profile` is the active route the avatar gets a coral ring (`box-shadow: 0 0 0 2px cream, 0 0 0 4px coral`) — that's the active affordance, not the bg. The `feed` icon shows a small coral dot (`badgeDot`, ink-bordered to pop against the pill) when `unreadChatCount > 0` and the user isn't on `/feed`. **The camera tab is gone** — snap capture is reached via the `+` avatar tile on `/feed`. The pill is **hidden** when `location.pathname.startsWith('/chat/')` so the chat input bar isn't covered. Add `pb-24` to any scrollable page so the pill doesn't overlap the last row.
- **Footer:** `Footer` is wrapped in `<div className="hidden md:block">` inside `AppShell`'s `<main>` so it only renders on desktop. The mobile log-out lives on the profile page instead.
- **Mobile log-out:** wide **coral** pill at the bottom of `/profile` (`md:hidden`), wired through `onLogout` prop threaded from `AppShell` → `Profile`. Desktop's log-out stays in the header.
- **Unread badges:** `AppShell` owns `unreadChatCount` and polls `GET /api/chats/unread/` every 30s while `currentUser` is set; the effect's dep list includes `location.pathname` so it also refetches immediately on every route change (catches up the badge after `/chat/<id>` marks read on mount). The same value drives both the desktop `feed` pill count chip and the mobile bottom-pill `feed` icon dot. On logout the badge nav doesn't render, so no stale value is shown.
- **Routing:** `App.jsx` extracts `AppShell` so `useNavigate`/`useLocation` work inside the Router. The home `/` route renders `WeekView` for logged-in users (and `Landing` otherwise). `/feed` and `/chat/:roomId` are protected routes. There is **no** Add item in the nav anymore — entry points are `+ add class` on `WeekView` and the `+` avatar tile on `/feed`. The App-level `SnapCaptureModal` mount + `mobileCaptureCourse` state were removed when the mobile camera tab was dropped; `/feed` owns its own capture modal.

**Layout:** Top navbar (`hidden md:flex`) + bottom pill (`md:hidden`); outer `<div>` is `flex-col h-screen` (no more `md:flex-row`). Body background is `cream` when logged in.
**Brand atoms:** Import from `@/components/shared/brand` — exports `T` (tokens), `FF` (fonts), `AppMark`, `Avatar`, `Blob`, `Squiggle`, `Star`, `PillBtn`, `MonoLabel`, `Chip`, `Icon`, `Wordmark`. `AppMark` is the canonical app icon — use it for every logo placement (top navbar, login/register, footer) so proportions stay locked.
**Colors:** **coral `#ED6A4A`** (primary, CTAs, live indicators), **lilac `#C8B0DF`** (secondary surfaces, friend accents), **lime `#C9EE6F`** (sticker pops only), **ink `#1F1A22`** (text + dark shells), **cream `#F8F4ED`** (base surface). Tailwind: `coral{,-dark,-light}`, `lilac{,-dark}`, `lime`, `ink{,-{60,40,15,8}}`, `cream`, `paper`. Legacy aliases `brand`/`brand-secondary`/`brand-accent` still resolve (now point at coral/lilac/lime) for any unmigrated screen. **Border radius:** the rebrand uses rounded-2xl/3xl cards and `rounded-full` pills freely — the old "no rounded-* except rounded-full" rule is dropped.
**Fonts:** **Bricolage Grotesque** (display — headings, wordmark, course titles; via `font-serif`/`font-display` or inline `style={{ fontFamily: FF.serif }}`), **Geist** (body, default), **Geist Mono** (labels, timestamps, code-like text — via `font-mono` or `<MonoLabel>`). Tone: low-caps, casual ("snap ur 9am", "ur week, ur ppl"). All three loaded from Google Fonts in `index.html`.
**Landing exception:** `Landing.jsx` and `header-navigation.tsx`/`mobile-header.tsx` are intentionally not fully reskinned yet — only font-family strings and primary color hex codes were swapped so they don't fall back to default fonts. Full reskin pending.

**Models:** `Course` (slots, assignments, exams), `FriendRequest`/`Friendship`, Custom `User` (university, major, grad_year, `last_seen` — nullable indexed datetime, stamped by `LastSeenMiddleware` at most once per 60s on authenticated requests), `Snap` (uploader/course FK, media_file, media_type, caption, visibility, is_removed, expires_at) + `SnapAudience` (snap/viewer FK, has_viewed, viewed_at — unique_together), `ChatRoom` (`room_type` `dm|group` default `dm`, `name` nullable, `created_by`, `linked_snap` nullable + unused in v1 UI, `is_active`), `ChatRoomMember` (`room`/`user` unique_together, `is_admin`, `last_read_at`), `Message` (`room`/`sender`, `content`, `reply_to` nullable, `is_removed`, `removed_at`, `created_at` — default ordering newest-first, index on `(room, -created_at)`). Soft-deleted messages keep their `content` row for future moderation; `MessageSerializer` blanks it client-side via `get_content`.

**Chat API:** Six endpoints under `/api/chats/`. `GET /` — my DM list (sorted unread → recent → room age) with flattened `other_user`, `last_message`, and `unread_count`. `POST /` `{friend_id}` — create-or-get DM, **friend-gated** (`_are_friends` helper requires `Friend.status=ACCEPTED` either direction; returns 403 `{detail:'not_friends'}` otherwise), deduped via `_find_existing_dm`. `GET /<id>/` — room + last 50 messages, descending. `GET /<id>/messages/?before=<msg_id>&limit=50` — paginated older messages. `POST /<id>/messages/` — text only, server trims, empty→400, >2000 chars→400. `DELETE /<id>/messages/<msg_id>/` — sender-only soft delete. `POST /<id>/read/` — bumps caller's `last_read_at`. `GET /unread/` — `{total: N}` summed across DMs (used by `App.jsx`'s 30s poll). **DM-only in v1**: `room_type='group'` enum value is reserved for a later phase; no group endpoints, no admin flags surfaced. `UserBlock` enforcement is **not yet wired** in `MessageCreateView` / `ChatCreateView` (lands with Phase 6/7 moderation).
**Storage:** Media via `django-storages[google]`. `STORAGES["default"]` toggles to `GoogleCloudStorage` when `GS_BUCKET_NAME` env is set (public URLs, `querystring_auth=False`, IAM-based ACL — bucket should grant `allUsers:objectViewer` for read). Unset locally → falls back to `MEDIA_ROOT`. Cloud Run FS is ephemeral; **prod must have `GS_BUCKET_NAME`**.
**Deploy:** Dockerfile `CMD` runs `python manage.py migrate --noinput && exec gunicorn …` so each new revision self-applies migrations on cold-start. `exec` matters — signals must reach gunicorn.
**External:** OpenAI (GPT-4o-mini, `OPENAI_API_KEY`), Resend (email), Sentry (errors), Cloud SQL (Postgres), Cloud Run (deploy), AdSense (`ca-pub-9825491172037028`, push in `useEffect`, needs `ads.txt`).
**Error UX:** Backend/server errors trigger a bottom-right toast (`ErrorToast.jsx`) via the `server-error` window event and `isErrorReportModalOpen` state in `App.jsx`. Toast auto-dismisses after 8s; links to `help@timetify.net`. No modal — do not reintroduce `ErrorReportModal`.

## Security

**Prod settings:** `SECRET_KEY` required (no `django-insecure-`), `SECURE_*`/HSTS/`SECURE_PROXY_SSL_HEADER` auto-enabled. `CORS_ALLOWED_ORIGINS` & `CSRF_TRUSTED_ORIGINS` are explicit allowlists; `ALLOWED_HOSTS` doesn't auto-expand.
**API:** `UserSerializer` read-only: `['id', 'username', 'email', 'status', 'shared_courses', 'last_seen', 'last_snap_at']` — don't loosen `username`/`email` without email verification (`major`/`grad_year` are writable via PATCH). `shared_courses` returns `course_id` strings the caller and target both have (computed per request). `last_snap_at` is a `SerializerMethodField` that reads a precomputed `last_snap_by_user_id` map from context (single `Max('created_at')` aggregate in `FriendListView.get_serializer_context`) — avoids N+1; returns `None` outside the friends-list path. `SearchFriend` username-only (no email search).
**Logging:** Use `logger = logging.getLogger(__name__)`, never `print()` — `send_default_pii=True` leaks data to Sentry.

## Privacy

`start_local.md` is gitignored and local-only. **Never** record, memorize, save to memory files, or include any content from `start_local.md` in responses, commits, or any other persistence mechanism.

## Work Style

1. Show thinking & assumptions before acting
2. List execution plan before file changes; wait for go-ahead
3. Explain *why* each change (problem/requirement), not what it does
